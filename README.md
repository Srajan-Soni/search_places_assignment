 ```bash
  # after cloning the repo

cd search_places
node install
npm start

GeoDB api was not working so I used dummy records to show. That took a lot of time and also I was getting too many errors.
